{
  "name": "study-sphere",
  "version": "1.0.0",
  "description": "Study Sphere Full Stack Application",
  "main": "index.js",
  "scripts": {
    "start": "npm install --prefix backend && npm install --prefix frontend && concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend -- --host\""
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
